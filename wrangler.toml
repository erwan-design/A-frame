name = "a-frame"
compatibility_date = "2026-09-14"

# Site statique servi directement (pas de code Worker) : les fichiers du dépôt sont
# publiés comme assets, sauf ceux listés dans .assetsignore (outils, relevés).
[assets]
directory = "."
